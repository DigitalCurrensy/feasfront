# Wiring
Ingest vehicle envelope + site list + DEM/horizon cache → Constraint seats (sun, Earth, slope, energy) → Gate → Frontier solver → Infeasibility brief.
No live SPICE in the browser. Precompute hours.
